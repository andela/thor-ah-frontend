import React from "react";
import './Loading.scss';

const Loading = () => (
  <section className='text-center' >
    <span className="loading__animation loading" />
  </section>
);

export default Loading;
